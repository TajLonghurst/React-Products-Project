import { useDispatch, useSelector } from "react-redux";
import { filterActions } from "../Store/filter-slice";

const useFilterCheckbox = (value) => {
  const onGenderChangeHandler = () => {};

  return;
};

export default useFilterCheckbox;
